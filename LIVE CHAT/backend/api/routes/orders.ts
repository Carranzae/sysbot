// Rutas de Orders - Backend API (Modularizado)
import ordersDomainRouter from '../../src/domains/orders/routes'

/**
 * Este archivo ahora es solo un proxy hacia el dominio modularizado.
 * La lógica real reside en backend/src/domains/orders/
 * Siguiendo el estándar de arquitectura Industrial Elite.
 */
export default ordersDomainRouter
