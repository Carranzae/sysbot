// Rutas de Settings - Backend API (Modularizado)
import settingsDomainRouter from '../../src/domains/settings/routes'

/**
 * Este archivo ahora es solo un proxy hacia el dominio modularizado.
 * La lógica real reside en backend/src/domains/settings/
 * Siguiendo el estándar de arquitectura Industrial Elite.
 */
export default settingsDomainRouter
