// Rutas de WhatsApp - Backend API (Modularizado)
import whatsappDomainRouter from '../../src/domains/whatsapp/routes'

/**
 * Este archivo ahora es solo un proxy hacia el dominio modularizado.
 * La lógica real reside en backend/src/domains/whatsapp/
 * Siguiendo el estándar de arquitectura Industrial Elite.
 */
export default whatsappDomainRouter
